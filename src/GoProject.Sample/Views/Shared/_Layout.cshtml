@{
    var version = "v" + typeof(MvcApplication).Assembly.GetName().Version.ToString(3);
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Go Project</title>

    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/goCss")

    @RenderSection("styles", required: false)

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/goJs")

</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a href="@Url.Action("Index", "Home")" class = "navbar-brand">Go MVC Project <small>@version</small></a>
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Page Flow", "PageFlowSample", "Go")</li>
                    <li>@Html.ActionLink("Double Tree", "DoubleTreeSample", "Go")</li>
                    <li>@Html.ActionLink("BPMN Editor", "BpmnEditorSample", "Go")</li>
                    <li>@Html.ActionLink("Pipes", "PipesSample", "Go")</li>
                    <li>@Html.ActionLink("Planogram", "PlanogramSample", "Go")</li>
                </ul>
            </div>
        </div>
    </div>
    <div>
        @RenderBody()
    </div>


    @RenderSection("scripts", required: false)
</body>
</html>