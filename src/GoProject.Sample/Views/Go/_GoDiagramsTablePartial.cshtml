@using GridMvc.Html
@model IEnumerable<GoProject.Diagram>

@Html.Grid(Model).Named("diagramsGrid").Columns(columns =>
{
    columns.Add(d => d.Id).Titled("Diagram Id").SetWidth(20).Sortable(true);
    columns.Add(d => d.Name).Titled("Diagram Name").SetWidth(110).Sortable(true);
}).WithPaging(10)